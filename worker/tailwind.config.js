/** @type {import('tailwindcss').Config} */
export default {
  // Scan the server-rendered views (classes live in html`` template literals).
  content: ["./src/**/*.{ts,js}"],
  theme: { extend: {} },
  plugins: [],
};
