db.sqlite3
*.pyc
.env
.env.uid
env/
.claude/settings.local.json

# Database dumps / backups — contain real user data, never commit.
# (Migration .sql files are committed; the generated seed is ignored in worker/.)
*.sql.bz2
