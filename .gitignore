# Dev-container / local secrets
.env
.env.uid
.env.*
!.env.example
!.env.test
.dev.vars
.claude/settings.local.json

# Database dumps / backups — contain real user data, never commit.
# (Migration .sql files are committed; the generated seed below is ignored.)
*.sql.bz2
seed.sql

# Node / SvelteKit / build output
node_modules
/.svelte-kit
/build
.output
.vercel
.netlify
.wrangler

# Generated by `wrangler types`
worker-configuration.d.ts

# Playwright
test-results

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# OS
.DS_Store
Thumbs.db
